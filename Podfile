# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'evgenAnswer' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'SwiftyDropbox'
  pod 'CoreXLSX', '~> 0.4.0'

  # Pods for evgenAnswer

  target 'evgenAnswerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'evgenAnswerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
